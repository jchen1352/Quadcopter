#include "stdafx.h"
#include <iostream>
#include "Quadcopter.h"

int main()
{
	uint8_t pitch = getPitch();
	uint8_t roll = getRoll();
	uint8_t height = getHeight();
	uint8_t FR = 0, FL = 0, BR = 0, BL = 0;

    return 0;
}
